Today the bot is still primarily a message-to-Pi bridge: Discord `MessageCreate` in, plain text prompt out, with per-channel persisted Pi sessions and streamed text edits back to Discord ([src/bot.ts:79](/Users/adam/Projects/discordjs-pi/src/bot.ts#L79), [src/pi-runtime.ts:189](/Users/adam/Projects/discordjs-pi/src/pi-runtime.ts#L189)). Against the documented Pi and `discord.js` surface, the biggest unused categories are:

- Discord-native interaction UX. The bot only listens to `Events.MessageCreate` and treats itself as “text-only right now” ([src/bot.ts:85](/Users/adam/Projects/discordjs-pi/src/bot.ts#L85), [src/bot.ts:136](/Users/adam/Projects/discordjs-pi/src/bot.ts#L136)). That means no slash commands, no REST command deployment, no ephemeral/deferred interaction replies, no buttons, no modals, and no structured admin flows. This is the largest unused `discord.js` capability area.

- Pi resource customization and project-specific behavior. `createAgentSession()` is being called with the default resource loader behavior plus an in-memory settings manager ([src/pi-runtime.ts:198](/Users/adam/Projects/discordjs-pi/src/pi-runtime.ts#L198), [src/pi-runtime.ts:93](/Users/adam/Projects/discordjs-pi/src/pi-runtime.ts#L93)). The repo has an `AGENTS.md`, so Pi will pick that up, but there are no project `.pi/` or `.agents/` resources at all, so you are not using Pi skills, prompt templates, extensions, custom system-prompt overrides, or custom context injection. The `.claude` docs are for local coding agents, not for the running bot.

- App-specific Pi tools and action layer. The current integration gives Pi a session, but there is no custom tool surface exposing Discord-aware operations or repo-specific workflows ([src/pi-runtime.ts:206](/Users/adam/Projects/discordjs-pi/src/pi-runtime.ts#L206)). So Pi is not being used as an action-taking bot with explicit tools like “send embed”, “summarize recent channel history”, “open thread”, “deploy command set”, or “moderate/respond with structured controls”.

- Multimodal and structured message handling. Routing only forwards trimmed message text plus lightweight metadata ([src/discord-routing.ts:35](/Users/adam/Projects/discordjs-pi/src/discord-routing.ts#L35)). Attachments, images, embeds, referenced messages, reactions, and richer Discord context are effectively ignored, and empty/non-text submissions get rejected ([src/bot.ts:126](/Users/adam/Projects/discordjs-pi/src/bot.ts#L126)). Pi’s image-capable prompt path is documented, but this bot does not use it.

- Advanced Pi session/agent control. You are using persistent sessions and queued `followUp()` calls, which is good, but not the richer session controls Pi exposes: `steer()` for interrupting a running turn, model/thinking-level changes, compaction control, branching/forking, tree navigation, labels, or persisted settings/auth/model configuration ([src/pi-runtime.ts:251](/Users/adam/Projects/discordjs-pi/src/pi-runtime.ts#L251), [src/pi-runtime.ts:68](/Users/adam/Projects/discordjs-pi/src/pi-runtime.ts#L68)). The current bot uses only the narrowest useful slice of Pi’s lifecycle API.

If you want the shortest prioritization: the biggest missing value is `discord.js` interactions/components first, then Pi custom tools/extensions second, then multimodal/context-rich prompting third.
